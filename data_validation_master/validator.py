# STEPS
# 1. List the posible values of each variable
# 2. For the categorical ones that are also "open input" question make a map using the unique values in data
# and asigning the most probably value from the list. Then replace in all the database

#import regex as re instead of import re
